objects -> collection of related propeties or method can represent real worl objects(animals,people,places)

const person2={

    name: "nowsath",
    lastname:"musaraf",
    age:22,
    isEmployee:false,
    // area:trincomalee -> area is key : trincomalee is value
    Hello:function(){
        console.log(`Hi, i'm ${this.lastname} `);
        //it's object function
    }
}

this->reference to the objects where is used
    console.log(`Hi, i'm ${this.lastname} `);
    //this is reference to the person2 object

constructors->special method to define the propeties and methods of objects

function person(name,lastname,age){
    this.age=age;
    this.name=name;
    this.lastname=lastname;
    //inside the constuctor we can create function also
    this.sayHello=function(){
        console.log(`Hi, i'm ${this.name} `)
    }
}
//constructors object
const person= new person("musaraf","nowsath",22)

classes
static keyword
inheritance
super
getter & setter
nested objects
Array off objects
sorting